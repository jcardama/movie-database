<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
	xmlns:app="http://schemas.android.com/apk/res-auto">
	<group>
		<item
			android:id="@+id/favorite_item"
			android:icon="@drawable/ic_favorite"
			app:showAsAction="ifRoom"
			app:iconTint="@color/textColorPrimaryInverse"
			android:title="@string/action_add_to_favorites" />
		<item
			android:id="@+id/watch_list_item"
			android:icon="@drawable/ic_playlist_add"
			app:showAsAction="ifRoom"
			app:iconTint="@color/textColorPrimaryInverse"
			android:title="@string/action_add_to_watch_list" />
	</group>
</menu>
